{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 1,
  "id": null,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": {
        "defaults": { "color": { "mode": "palette-classic" }, "unit": "reqps", "min": 0 },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "options": { "legend": { "displayMode": "list", "placement": "bottom" } },
      "targets": [
        { "expr": "sum by (job, method) (rate(reth_rpc_server_calls_started_total[5m]))", "legendFormat": "{{job}} {{method}}", "refId": "A" }
      ],
      "title": "RPC calls started (rate) – L1 reth & L2 base-reth",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": {
        "defaults": { "color": { "mode": "palette-classic" }, "unit": "reqps", "min": 0 },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "options": { "legend": { "displayMode": "list", "placement": "bottom" } },
      "targets": [
        { "expr": "sum by (job, method) (rate(reth_rpc_server_calls_failed_total[5m]))", "legendFormat": "{{job}} {{method}}", "refId": "A" }
      ],
      "title": "RPC calls failed (rate)",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": {
        "defaults": { "color": { "mode": "palette-classic" }, "unit": "s", "min": 0 },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "id": 3,
      "options": { "legend": { "displayMode": "list", "placement": "bottom" } },
      "targets": [
        { "expr": "reth_rpc_server_calls_time_seconds{quantile=\"0.99\"}", "legendFormat": "p99 {{job}} {{method}}", "refId": "A" },
        { "expr": "reth_rpc_server_calls_time_seconds{quantile=\"0.5\"}", "legendFormat": "p50 {{job}} {{method}}", "refId": "B" }
      ],
      "title": "RPC latency p50 / p99 (by job & method)",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": {
        "defaults": { "color": { "mode": "palette-classic" }, "unit": "short", "min": 0 },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "id": 4,
      "options": { "legend": { "displayMode": "list", "placement": "bottom" } },
      "targets": [
        { "expr": "sum by (job, method) (reth_rpc_server_calls_successful_total) or vector(0)", "legendFormat": "{{job}} {{method}}", "refId": "A" }
      ],
      "title": "RPC successful (total)",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": {
        "defaults": { "color": { "mode": "palette-classic" }, "unit": "short", "min": 0 },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 16 },
      "id": 5,
      "options": { "legend": { "displayMode": "list", "placement": "bottom" } },
      "targets": [
        { "expr": "sum by (job, method) (reth_rpc_server_calls_failed_total) or vector(0)", "legendFormat": "{{job}} {{method}}", "refId": "A" }
      ],
      "title": "RPC failed (total)",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "description": "Average latency = sum(call time) / count. Spikes can indicate slow L1/L2 or overload.",
      "fieldConfig": {
        "defaults": { "color": { "mode": "palette-classic" }, "unit": "s", "min": 0 },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 16 },
      "id": 6,
      "options": { "legend": { "displayMode": "list", "placement": "bottom" } },
      "targets": [
        { "expr": "sum by (job, method) (rate(reth_rpc_server_calls_time_seconds_sum[5m])) / sum by (job, method) (rate(reth_rpc_server_calls_time_seconds_count[5m]))", "legendFormat": "avg {{job}} {{method}}", "refId": "A" }
      ],
      "title": "RPC average latency (5m rate)",
      "type": "timeseries"
    }
  ],
  "refresh": "10s",
  "schemaVersion": 38,
  "style": "dark",
  "tags": ["rpc", "performance", "reth", "base-reth"],
  "templating": { "list": [] },
  "time": { "from": "now-1h", "to": "now" },
  "timepicker": {},
  "timezone": "browser",
  "title": "RPC & API Performance",
  "uid": "rpc-performance",
  "version": 1
}
