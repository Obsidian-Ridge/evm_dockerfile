# Provisioned Prometheus datasource (same Docker network).
# Grafana loads this on startup; no manual "Add data source" needed.
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    uid: prometheus
